import styled from 'styled-components';

export const Contant = styled.div`   

h1 {
        color: var(--red);
        text-decoration: none;
        font-weight: bold;

        &:hover {
            color: var(--red);
            font-weight: bold;
        }
    }
    
`; 